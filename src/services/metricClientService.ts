// src/services/MetricClientService.ts
import axios from 'axios';

const API_URL = 'http://localhost:3000'; // URL de tu API NestJS

// Clase para manejar la autenticación
class MetricClientService {

}

// Exporta una única instancia del servicio
export const metricClientService = new MetricClientService();